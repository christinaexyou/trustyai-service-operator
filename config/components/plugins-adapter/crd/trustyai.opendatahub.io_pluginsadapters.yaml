# YAML generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: pluginsadapters.trustyai.opendatahub.io
spec:
  group: trustyai.opendatahub.io
  names:
    kind: PluginsAdapter
    listKind: PluginsAdapterList
    plural: pluginsadapters
    singular: pluginsadapter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PluginsAdapter is the Schema for the pluginsadapters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PluginsAdapterSpec defines the desired state of PluginsAdapter
            properties:
              env:
                description: Env defines extra environment variables for the main
                  container.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: Name of the environment variable. Must be a C_IDENTIFIER.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              gatewayConfig:
                description: |-
                  GatewayConfig references the MCP Gateway whose namespace is used for
                  route / service validation.
                properties:
                  name:
                    description: Name of the MCP Gateway CR
                    type: string
                  namespace:
                    description: Namespace of the MCP Gateway CR
                    type: string
                type: object
              nemoConfig:
                description: |-
                  NemoConfig references an existing NeMo Guardrails CR whose Route will be
                  injected as DEFAULT_GUARDRAILS_SERVER_URL.
                properties:
                  name:
                    description: Name of the NeMo Guardrails CR
                    type: string
                  namespace:
                    description: Namespace of the NeMo Guardrails CR
                    type: string
                type: object
              pluginsConfigs:
                description: PluginsConfigs should be the names of the configmaps
                  containing Plugins Adapter server configuration files. All files
                  in PluginsConfigs will be mounted to /app/config/$Name
                items:
                  description: PluginConfig holds information related to a single
                    configuration of the Plugins Adapter server
                  properties:
                    configMaps:
                      description: ConfigMaps is a list of configmaps that comprise
                        the configuration. All files from these configmaps will be
                        mounted within /app/config/$Name
                      items:
                        type: string
                      type: array
                    default:
                      description: The Default flag determines whether config is treated
                        as the default config for the nemo-server. If no config is
                        set to default, the first entry in NemoConfigs will be used
                        as the default
                      type: boolean
                    name:
                      description: Name sets of the id of this particular config within
                        the Plugins Adapter server. This will create a directory called
                        /app/config/$Name. Since it $Name will be used a directory,
                        it must only contain alphanumeric characters, dashes, and
                        underscores.
                      type: string
                  type: object
                type: array
            required:
            - pluginsConfigs
            type: object
          status:
            description: PluginsAdapterStatus defines the observed state of PluginsAdapter
            properties:
              conditions:
                description: Conditions describes the state of the PluginsAdapter
                  resource.
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
